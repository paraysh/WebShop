@{
    // Legt das Layout für alle Ansichten fest, die von dieser Datei erben.
    // Das Layout befindet sich im Ordner "Views/Shared" und heißt "_Layout.cshtml".
    // Dadurch wird sichergestellt, dass alle Ansichten ein einheitliches Layout verwenden,
    // ohne dass in jeder Ansicht separat das Layout angegeben werden muss.
    Layout = "~/Views/Shared/_Layout.cshtml";
}